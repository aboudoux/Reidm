name: .NET

on:
  push:
    branches: [ test ]
  pull_request:
    branches: [ test ]

jobs:
  build:

    runs-on: self-hosted

    steps:    
    - name: script
      run: test.bat
    # - name: Restore dependencies
    #  run: dotnet restore
    # - name: Build
    #  run: dotnet build --no-restore
    # - name: Test
    #  run: dotnet test --no-build --verbosity normal
